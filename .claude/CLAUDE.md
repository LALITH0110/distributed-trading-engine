# Project Instructions

## Git

- NEVER push commits to GitHub (`git push` is forbidden unless explicitly requested)
- Local commits are fine — commit freely, but keep all changes local
- Only push when the user explicitly says "push" or "push to GitHub"
